{
  "name": "jest-manual-mock-bug",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "left-pad": "^1.1.3",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "start": "NODE_PATH=src/ react-scripts start",
    "build": "react-scripts build",
    "test": "NODE_PATH=src/ react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
